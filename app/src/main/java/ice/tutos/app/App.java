/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package ice.tutos.app;

import ice.tutos.list.LinkedList;

import static ice.tutos.utilities.StringUtils.join;
import static ice.tutos.utilities.StringUtils.split;
import static ice.tutos.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
